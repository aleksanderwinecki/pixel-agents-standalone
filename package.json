{
  "name": "pixel-agents-standalone",
  "description": "Pixel art office where your Claude Code agents come to life as animated characters",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "install:all": "cd webview-ui && npm install && cd ../standalone && npm install",
    "postinstall": "npm run install:all",
    "build": "cd standalone && npm run build",
    "start": "cd standalone && npm start"
  }
}
